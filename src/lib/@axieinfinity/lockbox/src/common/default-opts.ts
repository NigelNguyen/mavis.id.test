import { TrackingOpts } from "../types/wasm-params"

export const DEFAULT_TRACKING_OPTS: TrackingOpts = {
  enable: true,
  timeout: 3,
}
export const DEFAULT_OPTIONAL_PARAMS = '{"enableVerboseLog":false}'
